# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Google Generative AI API Key (Required)
# Get your API key from: https://aistudio.google.com/
GOOGLE_GENERATIVE_AI_API_KEY=your_google_ai_api_key_here

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Language Configuration
# Set default language for the application
# Supported values: 'en' (English), 'ru' (Russian)
# If not set, language will be auto-detected from system/browser
# NEXT_PUBLIC_DEFAULT_LANGUAGE=en

# Theme Configuration  
# Set default theme for the application
# Supported values: 'light', 'dark', 'system'
# If not set, defaults to 'system' (follows OS preference)
# NEXT_PUBLIC_DEFAULT_THEME=system

# =============================================================================
# CONFIGURATION EXAMPLES
# =============================================================================

# Example 1: English application with light theme
# NEXT_PUBLIC_DEFAULT_LANGUAGE=en
# NEXT_PUBLIC_DEFAULT_THEME=light

# Example 2: Russian application with dark theme
# NEXT_PUBLIC_DEFAULT_LANGUAGE=ru
# NEXT_PUBLIC_DEFAULT_THEME=dark

# Example 3: Auto-detection with system theme (default behavior)
# NEXT_PUBLIC_DEFAULT_LANGUAGE=en
# NEXT_PUBLIC_DEFAULT_THEME=system

# =============================================================================
# PRIORITY RULES
# =============================================================================

# Language Detection Priority:
# 1. User's manual selection (highest priority)
# 2. NEXT_PUBLIC_DEFAULT_LANGUAGE environment variable
# 3. Browser/system language detection (ru/be/uk → Russian, others → English)
# 4. Fallback to English

# Theme Detection Priority:
# 1. User's manual selection (highest priority)
# 2. NEXT_PUBLIC_DEFAULT_THEME environment variable
# 3. System/OS theme preference (light/dark)
# 4. Fallback to system theme